server:
  host: "0.0.0.0"
  port: 8080

database:
  dsn: "postgres://hysteria:hysteria@db:5432/hysteria?sslmode=disable"

jwt:
  secret: "change-this-to-a-random-secret-string"
  ttl: 86400

panel:
  admin_username: "admin"
  admin_password: "admin123"
  domain: "myduma.ru"
